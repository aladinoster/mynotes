#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;

void fill_vector(vector<int> &a)
{
    int value;
    for (int i = 0; i < 3; i++)
    {
        cin >> value;
    }
}

int main()
{
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */
    int N, Q, value;
    cin >> N;
    vector<int> pv(N);
    fill_vector(pv);

    return 0;
}
